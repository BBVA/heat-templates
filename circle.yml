## Export Openstack variables in order to authenticate into a public cloud

dependencies:
  pre:
    - pip install python-heatclient

test:
  override:
    - heat template-validate -e scaling/env.yml -f scaling/scale_down_up_vm_cpu.yml
    - heat template-validate -f heat_registry_proxy.yml
