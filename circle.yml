dependencies:
  pre:
    - pip install python-openstackclient

test:
  override:
    - openstack orchestration template validate -e scaling/env.yml -t scaling/scale_down_up_vm_cpu.yml
    - openstack orchestration template validate -t heat_registry_proxy.yml
